```sql
-- 5. 입출력 동시 감지
INSERT INTO dinai.guardrail_logs (id, employee_id, trace_id, ip_address, guardrail_id, 
guardrail_version, input_text, output_text, violation_details) VALUES ( 
    gen_random_uuid(), 'EMP001', 'b5c6d7e8f9a0b1c2', '10.0.1.55',
    '42dxjedldmfx', 'DRAFT',
    '내 번호 010-1234-5678인데 검색해줘',
    '검색 결과를 안내드립니다. {NAME}님의 요청에 따라...',
    '[{"label": "INPUT PII: PHONE", "action": "ANONYMIZED"},
      {"label": "OUTPUT PII: NAME", "action": "ANONYMIZED"}]'
);

-- 6. 파일 인제스트 PII 마스킹 (ApplyGuardrail API)
INSERT INTO dinai.guardrail_logs (id, employee_id, trace_id, ip_address, guardrail_id, 
guardrail_version, input_text, output_text, violation_details) VALUES (
    gen_random_uuid(), 'EMP004', 'c6d7e8f9a0b1c2d3', NULL,
    '42dxjedldmfx', 'DRAFT',
    '6166dbb7-75ce-47ad-9e66-ec205d16d4a5',
    '고객 {NAME}({KR_RESIDENT_REGISTRATION_NUMBER})의 {ADDRESS} 주소로 배송 예정',
    '[{"label": "INPUT PII: NAME", "action": "ANONYMIZED"},
      {"label": "INPUT regex: KR_RESIDENT_REGISTRATION_NUMBER", "action": "ANONYMIZED"},
      {"label": "INPUT PII: ADDRESS", "action": "ANONYMIZED"}]'
);
```


- 로그에 다음 항목 추가 저장 필요. 
	- 조인을 없애 비정형 데이터로 만들기 
	- username, deptName, deptCode 필요
	- web/mobile/api 를 위한 컬럼 필요 (필터에 필요) 
- 04-30 한 일 기록 
	- Aspect 로직 분석 
	- Notice 레거시 코드 제거 및 리펙토링 
	- access_log, audit_log 테이블 비정형 데이터로 만들기(조인 불필요하게끔)
	- 